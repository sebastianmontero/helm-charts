{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "auxiliar-prospects-api.fullname" . }}
  annotations:
    kubernetes.io/ingress.class: "nginx"    
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/proxy-body-size: 8m
spec:
  tls:
  - hosts:
    - {{ include "auxiliar-prospects-api.domain" . }}
    secretName: {{ include "auxiliar-prospects-api.fullname" . }}
  rules:
  - host: {{ include "auxiliar-prospects-api.domain" . }}
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: {{ include "auxiliar-prospects-api.fullname" . }}
            port:
              number: 3000
{{- end }}