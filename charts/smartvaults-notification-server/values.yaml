# Default values for hashed-faucet-server.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: docker pull sebastianmontero/smartvaults-notification-server-dev
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: 6256296b8c9d3acab65cd6d68011204a44a22b23

nameOverride: ""
fullnameOverride: ""

env:
  relays: ""
  apnTokenKeyLocalPath: ""
  apnTokenKeyId: "2R623TS6SC"
  apnTokenTeamId: "886J2SDR24"
  bundleId: "io.smartvaults.app"
  nodeEnv: "production"


resources:
  requests:
      cpu: 400m
      memory: 1Gi
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

smartvaults-server-db:
  postgresPassword: ""
  service:
    port: 5432
  
