apiVersion: v1
kind: Secret
metadata:
  name: {{ include "governance-db.fullname" . }}
  labels:
    {{- include "governance-db.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.postgresPassword }}