apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smartvaults-server-db.fullname" . }}-test-connection"
  labels:
    {{- include "smartvaults-server-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smartvaults-server-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
