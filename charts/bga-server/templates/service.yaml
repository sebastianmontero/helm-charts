apiVersion: v1
kind: Service
metadata:
  name: {{ include "bga-server.fullname" . }}
  labels:
    {{- include "bga-server.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bga-server.selectorLabels" . | nindent 4 }}
