apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bga-server.fullname" . }}-test-connection"
  labels:
    {{- include "bga-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bga-server.fullname" . }}:8080']
  restartPolicy: Never
