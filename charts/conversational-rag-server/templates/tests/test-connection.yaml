apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "conversational-rag-server.fullname" . }}-test-connection"
  labels:
    {{- include "conversational-rag-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "conversational-rag-server.fullname" . }}:8080']
  restartPolicy: Never
