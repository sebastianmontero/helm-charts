apiVersion: v1
kind: Service
metadata:
  name: {{ include "conversational-rag-server.fullname" . }}
  labels:
    {{- include "conversational-rag-server.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "conversational-rag-server.selectorLabels" . | nindent 4 }}
