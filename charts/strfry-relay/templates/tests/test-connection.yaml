apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "strfry-relay.fullname" . }}-test-connection"
  labels:
    {{- include "strfry-relay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "strfry-relay.fullname" . }}:7777']
  restartPolicy: Never
