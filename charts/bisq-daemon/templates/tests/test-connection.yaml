apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bisq-daemon.fullname" . }}-test-connection"
  labels:
    {{- include "bisq-daemon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bisq-daemon.fullname" . }}:9998']
  restartPolicy: Never
