apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "governance-chatbot-server.fullname" . }}-test-connection"
  labels:
    {{- include "governance-chatbot-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "governance-chatbot-server.fullname" . }}:3000']
  restartPolicy: Never
